/*
 * Adds MeowLib as a dependency
*/
include ':MeowLib'
project(':MeowLib').projectDir = new File(settingsDir, '../MeowLib')

/*
 * Adds SoftSPI as a dependency
*/
include ':SoftSPI'
project(':SoftSPI').projectDir = new File(settingsDir, '../../SoftSPI')